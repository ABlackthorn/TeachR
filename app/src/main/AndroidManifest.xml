<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teachr.teachr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/teachr_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/teachr_logo_rounded"
        android:supportsRtl="true"
        android:theme="@style/MaterialTheme">
        <activity
            android:name=".durationOfferActivity"
            android:label="@string/title_activity_duration_offer"></activity>
        <activity android:name=".PriceOfferActivity" />
        <activity
            android:name=".AdressOfferActivity"
            android:label="@string/title_activity_adress_offer" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity android:name=".matiereOfferActivity" />
        <activity
            android:name=".LastStepOfferActivity"
            android:label="@string/title_activity_last_step_offer" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".ListActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAvHwMXGx06mGfWTn5hWDyNLVFlX-rZtrg"/>

    </application>

</manifest>